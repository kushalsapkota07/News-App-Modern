<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsAppModern" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NewsAppModern" parent="Base.Theme.NewsAppModern" />


    <style name="AppBarTextStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/scarletRed</item>
        <item name="fontFamily">@font/poppins_bold</item>
    </style>

    <!-- Style for rounded ImageView -->
    <style name="roundedCornersShapeableImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <!-- Switch Style -->
    <style name="Widget.AppCompat.Switch" parent="Widget.Material3.CompoundButton.MaterialSwitch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="">
        <item name="colorPrimary">@color/scarletRed</item>
        <item name="colorSurfaceVariant">@color/black</item>
    </style>
</resources>